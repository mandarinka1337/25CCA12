Ѕазова€ структура  "ветвление".
ќбеспечивает в зависимости от результата проверки
услови€ (да или нет) выбор одного из альтернативных
путей работы алгоритма.  аждый из путей ведет к общему
 выходу, так что работа алгоритма будет продолжатьс€
 независимо от того, какой путь будет выбран. —труктура
  ветвление существует в четырех основных вариантах:

еслиЧто;     if условие then решение
еслиЧтоЧиначе;   if условие then решение else решение


¬ условии (в логическом выражении) используютс€ операторы отношени€.
–ассмотрим список операторов отношени€ :

больше >
меньше <
больше или равно в Pascal >=
меньше либо равно в Pascal <=
сравнение в Pascal =
не равно в Pascal <>
