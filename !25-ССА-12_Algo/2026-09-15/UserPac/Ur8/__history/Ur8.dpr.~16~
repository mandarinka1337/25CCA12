program Ur8;
// ”рок 8. ”словный оператор.
{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

  var
  a, b: byte;

  digmin, digmax, dig, max: byte;

  agemin, agemax, agest, aged: byte;

  res: boolean;

begin
{
  // составление и проверка условий (в логическом выражении)
  // сравнить переменные a и b. –езультат поместить в логическую переменную res
  // »нициализаци€ переменных
  a:=5;
  b:=12;
  // рассмотрим список список операторов отношени€:
  //сравнение в Pascal =
   res:= a=b;
  WriteLn('сравнение в Pascal = res:= a=b', res);
  //больше >
  res:= a>b;
  WriteLn('больше > res:= a>b', res);
  //меньше <
  res:= a<b;
  WriteLn('меньше < res:= a<b', res);
  //больше или равно в Pascal >=
  res:= a>=b;
  WriteLn('больше или равно в Pascal >= res:= a>=b', res);
  //меньше либо равно в Pascal <=
  res:= a<=b;
  WriteLn('меньше либо равно в Pascal <= res:= a<=b', res);
  //не равно в Pascal <>
  res:= a<>b;
  WriteLn('не равно в Pascal <> res:= a<>b;', res);
 }
  // построить логическое выражение провер€ющее вхождение переменной dig в промежуток значений 10-20
  // построить логическое выражение провер€ющее вхождение переменной dig в промежуок значений 10-20
  // и равно 10 и 20

  digmin:=10; digmax:=20;
  dig:=20;
  {
  res:=(dig>=digmin)and(Dig<=digmax);   // and логическое и выводит true если слева и справа trues
  WriteLn('–езультат услови€ dig в промежуток значений 10-20 = ', res);

  if (dig>=digmin)and(Dig<=digmax) then
  WriteLn('„исло dig ', dig,' находитс€ в промежуток значений 10-20 = ')
  else WriteLn('„исло dig ', dig,' не находитс€ в промежуток значений 10-20 = ');
   }
  // ¬озраст студента от 16 до 20 лет он обучаетс€ в колледже если менее 16 он школьник
  // если более 20 работает, если менее 6 лет ребенок


  // решение задачи через оператор если-то if условие then решение
  agemin:=16; agemax:=20;    aged:=6;
  agest:=20;
  {
  if (agest>=agemin) and (agest<=agemax) then
  WriteLn('¬озраст',agest,' он обучаетс€ в колледже');
  if (agest>agemax) then
  WriteLn('¬озраст',agest,' более 20 работает');
  // от 6 до 16
  if (agest>=aged) and (agest<=agemin) then
  WriteLn('¬озраст',agest,' он школьник');
  if (agest>=0) and (agest<=aged) then
  WriteLn('¬озраст от 0 до 6',agest,' он ребенок');
  // эти 4 услови€ проверки следует объединить в один оператор if с вложением
   }


  if (agest>=agemin) and (agest<=agemax) then
  WriteLn('¬озраст',agest,' он обучаетс€ в колледже') else
  if (agest>agemax) then
  WriteLn('¬озраст',agest,' более 20 работает') else
  // от 6 до 16
  if (agest>=aged) and (agest<=agemin) then
  WriteLn('¬озраст',agest,' он школьник') else
  if (agest>=0) and (agest<=aged) then
  WriteLn('¬озраст от 0 до 6',agest,' он ребенок');


  WriteLn(' онец программы урок 8');
  ReadLn; //точка остановки

end.
